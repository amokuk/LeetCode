66 Plus One 从后往前循环,如果发现一位小于9,直接加1返回,否则设置成0(9进1),如果跳出循环了(代表所有都为9,输入数组重新初始化为多一位(初始化后所有位都为0),第一位设置成1.

88 Merge Sorted Array
三指针从后往前, 把两个数组中的大的元素放到第一数组的最后(原理是如果第一数组中的原元素会被覆盖,那它一定会先被放置到后位去),如果结束后第二数组指针移动到头就结束(因为最后结果放到第一数组),如果没有要把剩余的元素复制到第一组数中(要以>=0作为判断复制,而不是>0)
